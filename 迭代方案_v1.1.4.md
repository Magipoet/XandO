# 2X&O 迭代方案 v1.1.x

> 文档版本：3.0  
> 迭代目标：v1.1.0 → v1.1.4  
> 提出日期：2026-05-03  
> 最后更新：2026-05-11  
> 状态：已完成（v1.1.4 已发布）

---

## 一、迭代概览

### 1.1 迭代目标

本次迭代针对用户体验、界面布局、可配置性三个维度进行功能增强，不涉及核心游戏规则修改。

### 1.2 需求清单

| 序号 | 需求模块 | 优先级 | 工作量预估 | 技术复杂度 |
|------|----------|--------|------------|------------|
| 1 | 撤销功能 | P0 - 阻塞 | 2h | 低 |
| 2 | 界面重构 | P0 - 阻塞 | 2h | 低 |
| 3 | 设置功能 | P1 - 重要 | 4h | 中 |
| 4 | 游戏说明弹窗 | P1 - 重要 | 1h | 低 |

### 1.3 依赖说明

- 现有技术栈：Flutter 3.41.9+, Riverpod 2.6.1+
- 新增依赖：shared_preferences ^2.1.0（用于设置持久化）

---

## 二、详细需求规格

### 需求1：撤销功能

#### 1.1 功能描述

实现游戏状态的撤销机制，允许玩家回到上一步的游戏状态。

#### 1.2 业务规则

| 规则编号 | 规则描述 | 验证点 |
|----------|----------|--------|
| R1.2.1 | 撤销操作将游戏状态恢复到上一次落子之前的状态 | X落子后点击撤销，棋盘恢复到X落子前 |
| R1.2.2 | 撤销可连续执行，直到回到初始棋盘状态 | 连续撤销直至所有棋子消失 |
| R1.2.3 | 点击"重新开始"后，撤销历史清空，无法再撤销到之前的状态 | 重新开始后，撤销按钮置灰不可用 |
| R1.2.4 | 游戏结束（某方获胜）后，禁止执行撤销操作 | 显示获胜对话框时，撤销按钮不可用 |

#### 1.3 交互设计

| 元素 | 描述 |
|------|------|
| 控件类型 | ElevatedButton（凸起按钮） |
| 文本 | "撤销" |
| 位置 | 底部按钮行，位于"重新开始"按钮左侧 |
| 对齐方式 | 两个按钮水平居中对齐 |
| 间距 | 按钮之间保留 16dp 间距（可根据视觉效果微调） |
| 禁用状态 | 无历史记录或游戏结束时，按钮背景色置灰，点击无响应 |

#### 1.4 技术实现方案

**状态管理扩展**：
- 在 `GameNotifier` 中维护一个 `List<GameState>` 类型的历史栈
- 每次执行 `makeMove()` 前，将当前状态 `copyWith()` 后入栈
- 执行 `undo()` 时，从栈顶弹出状态并恢复
- 执行 `resetGame()` 时，清空历史栈

**文件修改**：
| 文件路径 | 修改类型 | 修改内容 |
|----------|----------|----------|
| `lib/providers/game_provider.dart` | 修改 | 新增 `_history` 列表、`canUndo` getter、`undo()` 方法 |
| `lib/widgets/undo_button.dart` | 新增 | 撤销按钮组件，监听 `canUndo` 状态 |
| `lib/screens/game_screen.dart` | 修改 | 底部按钮行布局调整 |

---

### 需求2：界面重构

#### 2.1 功能描述

优化界面布局层次，增加游戏标题，调整状态栏位置。

#### 2.2 布局规格

**当前布局**：
```
┌─────────────────────────────────────────┐
│              轮到：×                     │  ← 状态栏（位置过高）
│                                         │
│           棋盘区域                      │
│                                         │
│              [ 重新开始 ]               │
└─────────────────────────────────────────┘
```

**目标布局**：
```
┌─────────────────────────────────────────┐
│  [空白]     动态井字棋       [?] [⚙]   │  ← 新增：标题栏（TopBar）
├─────────────────────────────────────────┤
│              轮到：×                     │  ← 状态栏（下移，位于标题栏下方）
├─────────────────────────────────────────┤
│                                         │
│           棋盘区域                      │
│                                         │
├─────────────────────────────────────────┤
│       [  撤销  ]    [ 重新开始 ]        │  ← 按钮行（并排布局）
└─────────────────────────────────────────┘
```

#### 2.3 样式规格

| 元素 | 字体大小 | 字重 | 说明 |
|------|----------|------|------|
| 标题"动态井字棋" | 24sp | FontWeight.bold | 大于状态栏字体 |
| 状态栏"轮到：×" | 18sp | FontWeight.bold | 保持现有大小 |

#### 2.4 间距规格

| 间距位置 | 数值 | 说明 |
|----------|------|------|
| 标题栏顶部边距 | 16dp | 距 SafeArea 顶部 |
| 标题栏与状态栏间距 | 8dp | 标题下方与状态栏上方 |
| 按钮行底部边距 | 24dp | 距 SafeArea 底部 |
| 撤销与重新开始按钮间距 | 16dp | 两按钮之间 |

#### 2.5 技术实现方案

**组件拆分**：
- `_buildTopBar()`：构建顶部标题栏，包含标题和右上角图标按钮
- `_buildActionButtons()`：构建底部按钮行，采用 Row 布局

**文件修改**：
| 文件路径 | 修改类型 | 修改内容 |
|----------|----------|----------|
| `lib/screens/game_screen.dart` | 修改 | 新增 TopBar 构建方法，调整整体布局结构 |
| `lib/constants/app_sizes.dart` | 修改 | 新增 `titleFontSize`、`titleTopMargin`、`titleToStatusSpacing`、`actionButtonSpacing` 常量 |

---

### 需求3：设置功能

#### 3.1 功能描述

实现可配置的界面参数，包括棋盘大小和棋子大小，并支持持久化保存。

#### 3.2 设置项规格

##### 3.2.1 棋盘大小设置

| 选项值 | 显示标签 | 比例说明 | 默认值 |
|--------|----------|----------|--------|
| standard | 标准款 | 屏幕宽度的 80% | 是 |
| large | 大款 | 屏幕宽度的 96% | 否 |

> 大款约为标准款的 1.2 倍（96% / 80% = 1.2），接近需求描述的"五分之六"（1.2）。

##### 3.2.2 棋子大小设置

棋子大小以**棋盘格子尺寸**为基准，按比例动态计算：

| 选项值 | 显示标签 | 格子占比 | 说明 | 默认值 |
|--------|----------|----------|------|--------|
| small | 小款 | 20% | 对应原有固定尺寸（约 1/5） | 否 |
| standard | 标准款 | 33.3%（1/3） | 新增，作为新默认 | 是 |
| large | 大款 | 50%（1/2） | 新增 | 否 |

**动态计算逻辑**：
```
格子尺寸 = (棋盘尺寸 - 内边距*2 - 格子间距*2) / 3
棋子字体大小 = 格子尺寸 × 棋子大小比例
```

#### 3.3 界面规格

**入口**：游戏界面右上角设置图标按钮（⚙）

**设置页面布局**：
```
┌─────────────────────────────────────────┐
│  < 返回          设置                    │  ← AppBar
├─────────────────────────────────────────┤
│                                         │
│  棋盘大小                               │
│  ○ 标准款    屏幕宽度的 80%            │
│  ● 大款      屏幕宽度的 96%            │
│                                         │
│  ─────────────────────────────────────  │
│                                         │
│  棋子大小                               │
│  ○ 小款      格子大小的 20%            │
│  ● 标准款    格子大小的 33%            │
│  ○ 大款      格子大小的 50%            │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │ 提示                              │  │
│  │ 设置会自动保存，下次打开游戏时   │  │
│  │ 会恢复您的偏好设置。             │  │
│  └───────────────────────────────────┘  │
│                                         │
└─────────────────────────────────────────┘
```

#### 3.4 持久化规则

| 规则 | 说明 |
|------|------|
| 存储时机 | 设置项变更时立即写入 |
| 存储介质 | shared_preferences（Flutter 官方持久化插件） |
| 读取时机 | 应用启动时读取并初始化 |
| 跨会话 | 重启应用后保留设置 |

#### 3.5 技术实现方案

**新增文件**：

| 文件路径 | 说明 |
|----------|------|
| `lib/models/board_size.dart` | 棋盘大小枚举 `enum BoardSize { standard, large }` 及 extension 方法 |
| `lib/models/piece_size.dart` | 棋子大小枚举 `enum PieceSize { small, standard, large }` 及 extension 方法 |
| `lib/providers/settings_provider.dart` | 设置状态管理，使用 `AsyncNotifier` 支持异步加载 |
| `lib/screens/settings_screen.dart` | 设置页面 |

**修改文件**：

| 文件路径 | 修改内容 |
|----------|----------|
| `lib/widgets/board_widget.dart` | 监听 `settingsProvider`，动态计算棋盘尺寸和棋子字体大小 |
| `lib/widgets/piece_widget.dart` | 改为接收 `pieceFontSize` 和 `orderNumberFontSize` 参数，移除固定尺寸 |
| `lib/screens/game_screen.dart` | 添加右上角设置图标按钮及导航逻辑 |
| `pubspec.yaml` | 添加 `shared_preferences: ^2.1.0` 依赖 |

**持久化存储键**：

| 键名 | 存储值类型 | 默认值 |
|------|------------|--------|
| `board_size` | int（枚举索引） | 0（BoardSize.standard） |
| `piece_size` | int（枚举索引） | 1（PieceSize.standard） |

---

### 需求4：游戏玩法说明弹窗

#### 4.1 功能描述

通过右上角帮助按钮（?）打开游戏玩法说明弹窗，用户可通过关闭按钮或点击空白区域关闭。

#### 4.2 内容规格

弹窗内容包含以下章节（参考现有游戏玩法说明文档）：

| 章节 | 内容要点 |
|------|----------|
| 游戏简介 | 创新机制说明：每方最多同时存在3个棋子，超过后自动移除最早放置的棋子 |
| 玩家 | X（×）先手，O（○）后手 |
| 棋盘 | 3×3 格子，共9个位置 |
| 基本规则 | 轮流落子、点击空白格子、三连获胜、连线方向（横/竖/斜） |
| 创新机制 | 最多3个棋子、移除规则、无限对战无平局 |
| 获胜条件 | 横向、纵向、斜向三连 |
| 策略提示 | 防守重要性、观察顺序、布局利用、重新开始、撤销功能 |

#### 4.3 交互规格

| 操作 | 行为 |
|------|------|
| 点击右上角 ? 图标 | 弹出 AlertDialog |
| 点击"关闭"按钮 | 关闭弹窗 |
| 点击弹窗外部区域（barrierDismissible） | 关闭弹窗 |
| Android 物理返回键 | 关闭弹窗 |

#### 4.4 技术实现方案

**新增文件**：
| 文件路径 | 说明 |
|----------|------|
| `lib/widgets/help_dialog.dart` | 帮助弹窗组件，使用 `AlertDialog` 封装 |

**修改文件**：
| 文件路径 | 修改内容 |
|----------|----------|
| `lib/screens/game_screen.dart` | 添加右上角帮助图标按钮及点击事件 |

---

## 三、文件变更清单

### 3.1 新增文件清单

| 文件路径 | 模块 | 说明 |
|----------|------|------|
| `lib/models/board_size.dart` | 数据模型 | 棋盘大小枚举定义 |
| `lib/models/piece_size.dart` | 数据模型 | 棋子大小枚举定义 |
| `lib/providers/settings_provider.dart` | 状态管理 | 设置状态管理及持久化 |
| `lib/widgets/undo_button.dart` | UI组件 | 撤销按钮 |
| `lib/widgets/help_dialog.dart` | UI组件 | 游戏玩法说明弹窗 |
| `lib/screens/settings_screen.dart` | 页面 | 设置页面 |

### 3.2 修改文件清单

| 文件路径 | 模块 | 修改内容 |
|----------|------|----------|
| `pubspec.yaml` | 配置 | 新增 shared_preferences 依赖 |
| `lib/providers/game_provider.dart` | 状态管理 | 新增撤销功能、历史栈 |
| `lib/widgets/board_widget.dart` | UI组件 | 支持动态尺寸计算 |
| `lib/widgets/piece_widget.dart` | UI组件 | 支持动态字体大小参数 |
| `lib/screens/game_screen.dart` | 页面 | 界面重构、新增图标按钮 |
| `lib/constants/app_colors.dart` | 常量 | 新增 buttonSecondary 颜色 |
| `lib/constants/app_sizes.dart` | 常量 | 新增标题、间距相关常量 |
| `lib/main.dart` | 入口 | 更新应用标题 |

### 3.3 不追踪文件

| 文件名 | 说明 |
|--------|------|
| `迭代方案.txt` | 原始需求草稿 |
| `迭代方案_v1.1.0.md` | 本文档（技术规格） |
| `报错.txt` | 临时调试文件 |

---

## 四、测试验收要点

### 4.1 功能测试

#### 撤销功能测试

| 用例编号 | 测试步骤 | 预期结果 |
|----------|----------|----------|
| UC1.1 | 初始状态点击"撤销" | 按钮置灰，无响应 |
| UC1.2 | X落子后点击"撤销" | 棋盘恢复到空状态，轮到X |
| UC1.3 | X落子、O落子后连续撤销两次 | 棋盘恢复到空状态 |
| UC1.4 | 落子多次后点击"重新开始" | 撤销按钮置灰，无法撤销 |
| UC1.5 | 一方获胜后 | 撤销按钮不可用 |

#### 界面布局测试

| 用例编号 | 测试步骤 | 预期结果 |
|----------|----------|----------|
| UC2.1 | 打开游戏 | 顶部显示"动态井字棋"标题 |
| UC2.2 | 观察标题和状态栏 | 标题字体大于状态栏字体 |
| UC2.3 | 观察右上角 | 显示帮助（?）和设置（⚙）两个图标按钮 |
| UC2.4 | 观察底部 | 撤销和重新开始按钮并排居中 |

#### 设置功能测试

| 用例编号 | 测试步骤 | 预期结果 |
|----------|----------|----------|
| UC3.1 | 点击右上角设置图标 | 进入设置页面 |
| UC3.2 | 选择棋盘"大款"返回游戏 | 棋盘尺寸变大（约为标准款的1.2倍） |
| UC3.3 | 选择棋子"大款"返回游戏 | 棋子字体变大（约为格子的1/2） |
| UC3.4 | 完全关闭应用后重新打开 | 设置保持上一次选择 |
| UC3.5 | 设置页面左上角返回按钮 | 返回游戏界面 |

#### 帮助弹窗测试

| 用例编号 | 测试步骤 | 预期结果 |
|----------|----------|----------|
| UC4.1 | 点击右上角 ? 图标 | 弹出玩法说明弹窗 |
| UC4.2 | 点击"关闭"按钮 | 弹窗关闭 |
| UC4.3 | 点击弹窗外部区域 | 弹窗关闭 |

### 4.2 构建测试

| 测试项 | 预期结果 |
|--------|----------|
| Windows 构建 | `flutter build windows --release` 成功 |
| Android 构建 | `flutter build apk --release` 成功 |
| 无编译警告 | 代码无语法错误、无类型错误 |

---

## 五、风险与注意事项

### 5.1 技术风险

| 风险项 | 影响 | 缓解措施 |
|--------|------|----------|
| shared_preferences 版本兼容性 | Android 构建失败 | 使用 ^2.1.0 版本，兼容 SDK 34 |
| Extension 导入问题 | 编译时报 getter 未定义 | 在使用处直接导入枚举文件，不依赖间接导入 |

### 5.2 注意事项

1. **枚举 Extension 导入**：Dart 的 Extension 方法不会通过间接导入可见，必须在使用该方法的文件中直接导入定义 Extension 的文件。

2. **依赖锁定**：`pubspec.lock` 文件应提交到版本控制，确保团队成员和 CI 环境使用相同的依赖版本。

3. **增量构建**：修改 `pubspec.yaml` 后需执行 `flutter pub get` 并允许 `pubspec.lock` 自动更新。

---

## 六、v1.1.1 问题修复（撤销按钮无法使用）

### 6.1 问题现象

v1.1.0 发布后发现撤销按钮存在以下问题：
- 撤销按钮始终呈现灰色（禁用状态）
- 点击撤销按钮无任何响应
- 即使玩家落子后，按钮仍无法使用

### 6.2 问题根因分析

#### 问题代码位置

**文件**：`lib/widgets/undo_button.dart`（v1.1.0 版本）

```dart
@override
Widget build(BuildContext context, WidgetRef ref) {
  final gameNotifier = ref.watch(gameProvider.notifier);
  final canUndo = gameNotifier.canUndo;
  // ...
}
```

#### 问题原因

**Riverpod 状态监听机制理解错误**：

| 代码写法 | 监听内容 | 是否触发重建 |
|----------|----------|-------------|
| `ref.watch(gameProvider.notifier)` | 监听 notifier 实例本身 | ❌ 状态变化时不重建 |
| `ref.watch(gameProvider)` | 监听 state 状态值 | ✅ 状态变化时重建 |

**问题流程分析**：

```
1. 初始状态
   - _history.isEmpty = true
   - canUndo = false
   - 按钮灰色禁用

2. 玩家落子
   - makeMove() 被调用
   - _history.add(state.copyWith()) 执行
   - state = 新状态
   - ✅ state 确实变化了

3. 问题关键点
   - UndoButton 只监听了 gameProvider.notifier
   - notifier 实例本身没有变化
   - ❌ build() 方法不会重新执行
   - canUndo 永远保持初始值 false

4. 结果
   - 按钮始终灰色
   - 点击无响应
```

### 6.3 技术原理详解

#### Riverpod 监听机制

```dart
// 方式1：监听 notifier 实例
final notifier = ref.watch(gameProvider.notifier);
// - 只在 notifier 实例创建时触发一次
// - 后续 state 变化不会触发重建
// - 适用于：只需要获取 notifier 引用，不需要响应状态变化

// 方式2：监听 state 状态
final state = ref.watch(gameProvider);
// - 每次 state 变化都会触发重建
// - 适用于：UI 需要根据状态变化更新

// 方式3：读取（不监听）
final notifier = ref.read(gameProvider.notifier);
// - 只读取一次，不建立监听关系
// - 适用于：回调函数中获取最新状态
```

#### 问题复现验证

```dart
// v1.1.0 错误代码
class UndoButton extends ConsumerWidget {
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    // ❌ 错误：只监听 notifier，不监听 state
    final gameNotifier = ref.watch(gameProvider.notifier);
    final canUndo = gameNotifier.canUndo;
    // canUndo 永远是初始值，不会更新
    
    return ElevatedButton(
      onPressed: canUndo ? ... : null,  // ❌ 永远是 null
      // ...
    );
  }
}
```

### 6.4 解决方案

#### 修复后的代码

**文件**：`lib/widgets/undo_button.dart`（v1.1.1 版本）

```dart
@override
Widget build(BuildContext context, WidgetRef ref) {
  // ✅ 正确：监听 state 变化，触发重建
  ref.watch(gameProvider);
  
  // ✅ 正确：使用 read 获取最新的 canUndo 值
  final canUndo = ref.read(gameProvider.notifier).canUndo;
  
  // ...
}
```

#### 修复原理

```
修复后流程：

1. 初始状态
   - ref.watch(gameProvider) 建立监听
   - canUndo = false
   - 按钮灰色禁用

2. 玩家落子
   - makeMove() 被调用
   - state 变化
   - ✅ ref.watch(gameProvider) 检测到变化
   - ✅ build() 方法重新执行

3. 重新计算
   - canUndo = ref.read(gameProvider.notifier).canUndo
   - _history.isNotEmpty = true
   - canUndo = true

4. 结果
   - 按钮变为可用状态
   - 点击可以正常撤销
```

### 6.5 修复验证

| 测试用例 | 修复前 | 修复后 |
|----------|--------|--------|
| 初始状态点击撤销 | 按钮灰色，无响应 | 按钮灰色，无响应（正确） |
| X落子后点击撤销 | 按钮仍灰色 | 按钮可用，棋盘恢复 |
| 连续落子后撤销 | 无法使用 | 可连续撤销 |
| 重新开始后 | - | 按钮恢复灰色 |

### 6.6 经验教训

1. **Riverpod 监听区分**：
   - `ref.watch(provider)` - 监听状态变化，UI 响应
   - `ref.watch(provider.notifier)` - 监听 notifier 实例，极少使用
   - `ref.read(provider)` - 读取一次，不监听

2. **调试建议**：
   - 状态变化时检查 `build()` 是否被调用
   - 使用 `print` 或断点确认数据流

3. **代码审查要点**：
   - 检查 `ref.watch` 的监听对象
   - 确认 UI 组件是否正确响应状态变化

### 6.7 版本更新

#### 版本号变更

| 文件 | 原版本 | 新版本 |
|------|--------|--------|
| `pubspec.yaml` | 1.0.0+1 | 1.1.1+1 |

#### Git 提交记录

```bash
# 提交修复
git add lib/widgets/undo_button.dart pubspec.yaml
git commit -m "fix: 修复撤销按钮无法使用问题

问题原因: undo_button.dart 中 ref.watch(gameProvider.notifier)
         只监听 notifier 实例，状态变化时不触发重建
解决方案: ref.watch(gameProvider) 监听状态变化
版本: 1.1.1+1"

# 推送
git push origin master

# 创建 Tag
git tag -a v1.1.1 -m "v1.1.1 修复撤销按钮问题"
git push origin v1.1.1
```

---

## 八、v1.1.2 棋子大小调整

### 8.1 问题现象

v1.1.1 发布后发现棋子显示太小，用户反馈视觉效果不佳。

### 8.2 需求规格

#### 8.2.1 棋子大小调整

| 选项值 | 修改前比例 | 修改后比例 | 说明 |
|--------|-----------|-----------|------|
| small | 20% | 33.3% (1/3) | 原标准款改为小款 |
| standard | 33.3% (1/3) | 50% (1/2) | 原大款改为标准款（默认） |
| large | 50% (1/2) | 66.7% (2/3) | 新增大款 |

> 原有的 20% 小款已不再使用。

### 8.3 技术实现方案

**修改文件**：

| 文件路径 | 修改内容 |
|----------|----------|
| `lib/models/piece_size.dart` | 调整 `cellRatio` 比例值 |

**修改后的代码**：

```dart
extension PieceSizeExtension on PieceSize {
  double get cellRatio {
    switch (this) {
      case PieceSize.small:
        return 1.0 / 3.0;   // 33.3%
      case PieceSize.standard:
        return 0.5;         // 50%
      case PieceSize.large:
        return 2.0 / 3.0;   // 66.7%
    }
  }
}
```

### 8.4 版本更新

| 文件 | 原版本 | 新版本 |
|------|--------|--------|
| `pubspec.yaml` | 1.1.1+1 | 1.1.2+1 |

---

## 九、v1.1.3 标题居中与布局崩溃修复

### 9.1 问题现象

v1.1.2 尝试实现标题居中时出现布局崩溃：
- 标题栏、状态栏、按钮全部消失
- 棋盘只显示在左上角，没有居中
- 游戏几乎无法使用

### 9.2 问题根因分析

#### 错误的布局方案

```dart
// ❌ 错误：Stack 在无约束的 Column 中
Widget _buildTopBar() {
  return Padding(
    padding: const EdgeInsets.only(...),
    child: Stack(
      alignment: Alignment.center,
      children: [
        const Positioned.fill(  // ❌ 尝试填充父组件所有空间
          child: Center(child: Text('动态井字棋')),
        ),
        Positioned(right: 0, child: ...),
      ],
    ),
  );
}
```

#### 问题原因

**Flutter 布局约束问题**：

| 问题点 | 说明 |
|--------|------|
| `Column` 的子组件 | 默认无约束（高度由内容决定） |
| `Stack` 在 `Column` 中 | 没有明确的高度约束 |
| `Positioned.fill` | 尝试填充父组件所有可用空间 |
| 冲突 | 在无约束的 `Column` 中，导致无限高度，布局崩溃 |

### 9.3 解决方案

#### 修复后的代码

**文件**：`lib/screens/game_screen.dart`

```dart
Widget _buildTopBar() {
  return Padding(
    padding: const EdgeInsets.only(...),
    child: SizedBox(
      width: double.infinity,  // 宽度充满屏幕
      height: 48.0,            // ✅ 明确的高度约束
      child: Stack(
        alignment: Alignment.center,  // 子组件默认居中
        children: [
          const Text('动态井字棋', ...),  // ✅ 利用 alignment 居中
          Positioned(
            right: 0,  // ✅ 固定在右侧
            child: Row(/* 图标按钮 */),
          ),
        ],
      ),
    ),
  );
}
```

#### 关键改动

| 改动 | 说明 |
|------|------|
| 添加 `SizedBox` | 给 `Stack` 明确的尺寸约束 |
| 移除 `Positioned.fill` | 避免无限高度问题 |
| 利用 `Stack.alignment` | 子组件默认居中 |

### 9.4 修复验证

| 测试用例 | 修复前 | 修复后 |
|----------|--------|--------|
| 打开游戏 | 布局崩溃，只看到棋盘 | 标题、状态栏、按钮正常显示 |
| 标题位置 | 被右侧图标挤偏 | 真正居中显示 |
| 游戏功能 | 无法使用 | 正常可用 |

### 9.5 经验教训

1. **`Stack` 需要明确约束**：
   - 在 `Column`、`ListView` 等可滚动容器中，必须给 `Stack` 明确的尺寸约束
   - 使用 `SizedBox` 或 `Container` 包裹

2. **`Positioned.fill` 使用场景**：
   - 仅适用于父组件有明确尺寸约束的情况
   - 在无约束环境中会导致无限尺寸，引发崩溃

3. **`Stack.alignment` 的正确使用**：
   - 不需要 `Positioned` 的子组件会遵循 `alignment` 属性
   - 这是实现真正居中的最佳方式

### 9.6 版本更新

| 文件 | 原版本 | 新版本 |
|------|--------|--------|
| `pubspec.yaml` | 1.1.2+1 | 1.1.3+1 |

---

## 十、v1.1.4 棋子显示逻辑优化

### 10.1 问题现象

v1.1.3 中棋子显示逻辑不符合用户直觉：
- 放下第一个棋子时，标号为 1，但颜色很浅（50% 透明度）
- 第二个棋子放下时，颜色也比较浅
- 用户期望：刚放下的棋子颜色最深，后续逐渐变浅

### 10.2 问题根因分析

#### 当前逻辑（修改前）

**`relativeOrder` 语义**：

| `relativeOrder` | 语义 | 透明度 | 显示效果 |
|-----------------|------|--------|----------|
| 1 | 最老的棋子 | `opacityOldest` = 0.5 | 最浅 |
| 2 | 中间的棋子 | `opacityMiddle` = 0.75 | 中间 |
| 3 | 最新的棋子 | `opacityNewest` = 1.0 | 最深 |

**问题分析**：

- 用户看到**序号 1** 时，直觉认为是"第一个放下的"、"最新的"
- 但实际逻辑中，`relativeOrder = 1` 表示"最老的"棋子
- 导致视觉效果与用户预期相反

#### 透明度映射代码（修改前）

**文件**：`lib/widgets/piece_widget.dart`

```dart
double _getOpacity() {
  if (piece == null) return 1.0;
  switch (piece!.relativeOrder) {
    case 1:
      return AppColors.opacityOldest;   // 0.5 - 最浅 ❌
    case 2:
      return AppColors.opacityMiddle;   // 0.75
    case 3:
      return AppColors.opacityNewest;   // 1.0 - 最深
    default:
      return AppColors.opacityNewest;
  }
}
```

### 10.3 解决方案

让 **序号语义与用户直觉一致**：

| `relativeOrder` | 新语义 | 透明度 | 显示效果 |
|-----------------|--------|--------|----------|
| 1 | 最新的棋子（刚放下） | `opacityNewest` = 1.0 | **最深** ✓ |
| 2 | 中间的棋子 | `opacityMiddle` = 0.75 | 中间 |
| 3 | 最老的棋子（即将移除） | `opacityOldest` = 0.5 | **最浅** ✓ |

### 10.4 技术实现方案

#### 1. 修改 `placePiece` 方法

**文件**：`lib/services/board_service.dart`

```dart
Piece? placePiece(Board board, Player player, int row, int col) {
  if (!board.isEmpty(row, col)) return null;

  if (board.getPieceCount(player) >= GameRules.maxPiecesPerPlayer) {
    final oldestPiece = board.playerPieces[player]!.first;
    _removePiece(board, oldestPiece);
    // 不再调用 _updateRelativeOrders
  }

  final newPiece = Piece(
    // ...
    relativeOrder: 1,  // 新棋子序号 = 1（最新）
    // ...
  );

  _incrementExistingOrders(board, player);  // 新增方法
  _addPiece(board, newPiece);

  return newPiece;
}
```

#### 2. 新增 `_incrementExistingOrders` 方法

```dart
void _incrementExistingOrders(Board board, Player player) {
  final pieces = board.playerPieces[player]!;
  for (int i = 0; i < pieces.length; i++) {
    pieces[i] = pieces[i].copyWith(
      relativeOrder: pieces[i].relativeOrder + 1,  // 序号 +1
    );
    
    final cellPiece = board.cells[pieces[i].row][pieces[i].col];
    if (cellPiece != null && cellPiece.id == pieces[i].id) {
      board.cells[pieces[i].row][pieces[i].col] = pieces[i];
    }
  }
}
```

#### 3. 修改 `_updateRelativeOrders` 方法

```dart
void _updateRelativeOrders(Board board, Player player) {
  final pieces = board.playerPieces[player]!;
  for (int i = 0; i < pieces.length; i++) {
    // 原：i + 1 → 最老的=1，最新的=3
    // 新：pieces.length - i → 最老的=3，最新的=1
    pieces[i] = pieces[i].copyWith(
      relativeOrder: pieces.length - i,
    );
    // ...
  }
}
```

#### 4. 修改透明度映射

**文件**：`lib/widgets/piece_widget.dart`

```dart
double _getOpacity() {
  if (piece == null) return 1.0;
  switch (piece!.relativeOrder) {
    case 1:
      return AppColors.opacityNewest;   // 1.0 - 最深 ✓
    case 2:
      return AppColors.opacityMiddle;   // 0.75
    case 3:
      return AppColors.opacityOldest;   // 0.5 - 最浅 ✓
    default:
      return AppColors.opacityNewest;
  }
}
```

### 10.5 修改后的效果

| 操作 | 棋子状态 | 序号 | 透明度 | 颜色 |
|------|----------|------|--------|------|
| 放下第1个 | 最新 | 1 | 1.0 | **最深** ✓ |
| 放下第2个 | 新棋子 | 1 | 1.0 | **最深** ✓ |
| | 第1个 | 2 | 0.75 | 变浅 |
| 放下第3个 | 新棋子 | 1 | 1.0 | **最深** ✓ |
| | 第2个 | 2 | 0.75 | 中间 |
| | 第1个 | 3 | 0.5 | **最浅** ✓ |
| 放下第4个 | 移除第1个，新棋子 | 1 | 1.0 | **最深** ✓ |

### 10.6 关于 `_updateRelativeOrders` 未被调用的说明

**问题**：IDE 中 `_updateRelativeOrders` 方法下方有黄色波浪线，提示未被调用。

**分析**：

```dart
// v1.1.4 修改前（v1.1.1 版本）
Piece? placePiece(Board board, Player player, int row, int col) {
  if (board.getPieceCount(player) >= GameRules.maxPiecesPerPlayer) {
    final oldestPiece = board.playerPieces[player]!.first;
    _removePiece(board, oldestPiece);
    _updateRelativeOrders(board, player);  // ← 移除后重新编号
  }
  // ...
}

// v1.1.4 修改后
Piece? placePiece(Board board, Player player, int row, int col) {
  if (board.getPieceCount(player) >= GameRules.maxPiecesPerPlayer) {
    final oldestPiece = board.playerPieces[player]!.first;
    _removePiece(board, oldestPiece);
    // ← 不再调用 _updateRelativeOrders
  }
  _incrementExistingOrders(board, player);  // 新增：已有棋子序号 +1
  // ...
}
```

**结论**：

- `_updateRelativeOrders` 方法在 v1.1.4 中确实**没有被调用**
- 这是**预期行为**，因为我们改用了 `_incrementExistingOrders` 方法
- **黄色波浪线无影响**，只是 IDE 的代码检查提示
- 如果希望消除警告，可以删除 `_updateRelativeOrders` 方法

### 10.7 版本更新

| 文件 | 原版本 | 新版本 |
|------|--------|--------|
| `pubspec.yaml` | 1.1.3+1 | 1.1.4+1 |

---

## 十一、版本信息（更新）

### 11.1 版本历史（完整）

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v1.0.6 | - | 基础版本 |
| v1.1.0 | 2026-05-04 | 新增撤销功能、界面重构、设置功能、帮助弹窗 |
| v1.1.1 | 2026-05-04 | **修复撤销按钮无法使用问题**<br>- 原因：`undo_button.dart` 中状态监听方式错误<br>- 修复：`ref.watch(gameProvider.notifier)` → `ref.watch(gameProvider)` |
| v1.1.2 | 2026-05-05 | **棋子大小调整**<br>- small: 33.3% (1/3)<br>- standard: 50% (1/2) → 默认值<br>- large: 66.7% (2/3) → 新增 |
| v1.1.3 | 2026-05-05 | **修复标题居中导致的布局崩溃**<br>- 原因：`Stack` 在无约束 `Column` 中使用 `Positioned.fill`<br>- 修复：用 `SizedBox` 给 `Stack` 明确的尺寸约束 |
| v1.1.4 | 2026-05-11 | **棋子显示逻辑优化**<br>- 原因：`relativeOrder` 语义与用户直觉相反<br>- 修复：序号 1 = 最新（最深），序号 3 = 最老（最浅） |

### 11.2 文档信息（更新）

| 项目 | 内容 |
|------|------|
| 文档版本 | 3.0 |
| 迭代目标版本 | v1.1.0 → v1.1.4 |
| 文档起草日期 | 2026-05-03 |
| 文档最后更新 | 2026-05-11 |
| 实现状态 | 已完成（v1.1.4 已发布） |
