# Flutter 多平台统一构建方案（v2.0 改进版）

## 文档信息
- **创建日期**：2026-05-03
- **最后更新**：2026-05-03
- **适用项目**：tictactoe_game
- **方案版本**：v2.0

---

## 一、方案概述

### 1.1 目标
为 tictactoe_game Flutter 项目配置 GitHub Actions，实现：
1. **统一构建**：Windows 和 Android 使用同一个 Workflow，消除竞争条件
2. **自动构建**：推送标签自动触发双平台构建
3. **自动签名**：Android APK 使用您的密钥自动签名
4. **统一发布**：同一个 GitHub Release 包含双平台安装包
5. **预检查机制**：构建前自动验证配置完整性
6. **签名验证**：构建后自动验证 APK 签名正确性

### 1.2 难度评估
| 评估项 | 等级 | 说明 |
|--------|------|------|
| **整体难度** | ⭐⭐☆☆☆ 中等 | 需要配置签名，但步骤清晰 |
| **预计时间** | 2-3 小时 | 包含配置、测试、调试 |
| **技术要求** | 基础命令行操作 | 无需 Android 开发经验 |

### 1.3 方案选择（v2.0 改进版）
| 选项 | 选择 | 说明 |
|------|------|------|
| APK 类型 | **Release APK** | 性能更好、体积更小 |
| Workflow 类型 | **统一 Workflow + Jobs 依赖** | 消除竞争条件，确保顺序执行 |
| 上传策略 | **统一 Release Job** | 只有双平台都构建成功才创建 Release |
| 版本号管理 | **自动从 Tag 推导** | 无需手动修改 pubspec.yaml |
| build.gradle 修改 | **模板文件替换** | 比 sed 更可靠，不依赖格式不变 |
| 预检查 | **新增** | 构建前验证所有配置 |
| 签名验证 | **新增** | 构建后验证 APK 签名 |

---

## 二、v2.0 改进说明

### 2.1 为什么需要改进？

#### 原方案的问题
1. **竞争条件**：两个独立 Workflow 同时触发，可能导致 Release 被创建两次
2. **sed 脆弱性**：依赖 `flutter create` 生成的 build.gradle 格式不变，版本升级可能失效
3. **无预检查**：构建开始后才发现 Secrets 未配置，浪费时间
4. **无签名验证**：无法确认 APK 是否正确签名
5. **版本号需手动**：每次发布需要手动修改 pubspec.yaml

#### v2.0 解决方案
```
┌─────────────────────────────────────────────────────────────────┐
│                    统一 Workflow (build-all.yml)                 │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  pre-check Job (预检查)                                   │   │
│  │  - 验证 Tag 格式                                          │   │
│  │  - 验证 pubspec.yaml 版本                                │   │
│  │  - 验证 GitHub Secrets 配置                              │   │
│  │  - 验证模板文件存在                                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                    │
│                              ▼                                    │
│        ┌───────────────────┴───────────────────┐                │
│        │                                       │                │
│        ▼                                       ▼                │
│  ┌───────────────┐                   ┌─────────────────┐       │
│  │ build-windows │                   │ build-android   │       │
│  │ (并行执行)     │                   │ (并行执行)       │       │
│  │               │                   │                 │       │
│  │ 1. 配置 Flutter│                   │ 1. 配置 Java+SDK │       │
│  │ 2. 构建 exe    │                   │ 2. 配置 Flutter  │       │
│  │ 3. 打包 zip    │                   │ 3. 替换模板文件  │       │
│  │ 4. 上传 Artifact│                   │ 4. 配置签名      │       │
│  │               │                   │ 5. 构建 APK      │       │
│  │               │                   │ 6. 验证签名      │       │
│  │               │                   │ 7. 上传 Artifact │       │
│  └───────────────┘                   └─────────────────┘       │
│        │                                       │                │
│        └───────────────────┬───────────────────┘                │
│                            │                                     │
│                            ▼                                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  create-release Job (依赖前两个 Job 都成功)              │   │
│  │  - 下载 Windows Artifact                                 │   │
│  │  - 下载 Android Artifact                                 │   │
│  │  - 创建统一的 GitHub Release                             │   │
│  │  - 自动生成 Release Notes                                │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 关键改进点对比

| 改进项 | v1.0 (原方案) | v2.0 (改进版) | 优势 |
|--------|---------------|---------------|------|
| **Workflow 架构** | 两个独立文件 | 统一文件 + Jobs 依赖 | 消除竞争条件 |
| **build.gradle 修改** | sed 动态修改 | 模板文件替换 | 不依赖格式，更可靠 |
| **预检查** | 无 | 新增 pre-check Job | 提前发现配置问题 |
| **签名验证** | 无 | 新增验证步骤 | 确认 APK 正确签名 |
| **版本号管理** | 需手动修改 | 自动从 Tag 推导 | 减少人为错误 |
| **Release 创建** | 两个 Job 都尝试创建 | 统一 create-release Job | 确保只创建一次 |

---

## 三、关键概念

### 3.1 为什么需要签名？

Android 系统要求所有应用必须经过数字签名才能安装。签名的作用：
1. **身份验证**：确认应用开发者的身份
2. **完整性验证**：确保应用在传输过程中没有被篡改
3. **版本管理**：同一签名的应用才能覆盖安装更新

### 3.2 Debug vs Release 签名对比

| 对比项 | Debug 签名 | Release 签名 |
|--------|-----------|-------------|
| **签名来源** | Android SDK 自动生成 | 开发者自己创建 |
| **安全性** | 低（所有人都有相同的 debug key） | 高（只有你有私钥） |
| **应用商店** | 不能上传 | 必须使用 |
| **安装限制** | 只能在调试模式安装 | 可正常安装 |
| **更新覆盖** | 不同机器的 debug key 不能覆盖 | 同一签名可以覆盖更新 |

### 3.3 签名文件说明

我们需要创建一个 **Java KeyStore (JKS)** 文件，包含：
- `keystore.jks`：密钥库文件（包含私钥和证书）
- `key.properties`：配置文件（包含密码和别名，不提交到 git）

### 3.4 版本号自动管理原理

```yaml
# 从 git tag 自动提取版本号
Tag: v1.0.3
     ↓
提取: 1.0.3 (去掉 v 前缀)
     ↓
用于: APK 文件名、Release 命名
```

**优势**：
- 无需手动修改 pubspec.yaml
- Tag 是唯一的版本标识
- 符合语义化版本规范

---

## 四、详细实施方案

### 4.1 文件结构（新增/修改）

```
tictactoe_game/
├── .github/
│   └── workflows/
│       ├── build-all.yml          ✅ 新增（统一 Workflow）
│       ├── build-windows.yml      ⚠️ 可保留（备用）
│       └── build-android.yml      ⚠️ 可保留（备用）
├── config/
│   └── android/
│       └── app-build.gradle.template ✅ 新增（签名配置模板）
├── .gitignore                      ✅ 已更新（添加签名文件忽略）
├── pubspec.yaml                    ⚠️ 需确认版本号
└── 安卓方案.md                     ✅ 已更新（本文档）
```

---

### 阶段一：本地准备（约 30 分钟）

#### 步骤 1.1：检查 Java 环境

Release APK 签名需要 Java JDK。让我检查当前环境：

```bash
# 检查 Java 版本
java -version

# 检查 keytool 工具（用于创建签名）
which keytool
```

#### 步骤 1.2：创建签名密钥库 (Keystore)

这是最关键的一步。我们需要创建一个密钥库文件。

**⚠️ 重要提示**：
- 请记住所有密码和别名，后续需要用到
- 密钥库文件非常重要，请妥善备份
- 不要将密钥库文件或密码提交到公开的 GitHub 仓库

**操作命令**：

```bash
cd /remote-home/share/lijl/task_all/2XandO/tictactoe_game

# 交互式创建（会提示你输入信息）
keytool -genkey -v \
  -keystore tictactoe-keystore.jks \
  -alias tictactoe-key \
  -keyalg RSA \
  -keysize 2048 \
  -validity 36500
```

**交互式问答示例**：
```
输入密钥库口令:  （输入一个密码，记住它）
再次输入新口令:  （再次输入相同密码）

您的名字与姓氏是什么?
  [Unknown]:  TicTacToe Game
  
您的组织单位名称是什么?
  [Unknown]:  Personal Project
  
您的组织名称是什么?
  [Unknown]:  Personal
  
您所在的城市或区域名称是什么?
  [Unknown]:  Beijing
  
您所在的省/市/自治区名称是什么?
  [Unknown]:  Beijing
  
该单位的双字母国家/地区代码是什么?
  [Unknown]:  CN
  
CN=My Name, OU=My Org Unit, O=My Org, L=My City, ST=My State, C=CN 是否正确?
  [否]:  y  （输入 y 确认）

输入 <tictactoe-key> 的密钥口令
	(如果和密钥库口令相同, 按回车):  （按回车，使用相同密码）
```

#### 步骤 1.3：验证密钥库创建成功

```bash
# 查看密钥库内容
keytool -list -v -keystore tictactoe-keystore.jks
```

如果成功，你会看到类似这样的输出：
```
密钥库类型: PKCS12
密钥库提供方: SUN

您的密钥库包含 1 个条目

别名名称: tictactoe-key
创建日期: 2026-5-3
条目类型: PrivateKeyEntry
证书链长度: 1
证书[1]:
...
```

#### 步骤 1.4：创建 key.properties 配置文件（用于本地测试）

在项目根目录创建 `key.properties` 文件（仅用于本地测试，**不会提交**到 git）：

```bash
cd /remote-home/share/lijl/task_all/2XandO/tictactoe_game

# 创建 key.properties 文件（替换为你实际的密码）
cat > key.properties << 'EOF'
storePassword=你的密钥库密码
keyPassword=你的密钥密码
keyAlias=tictactoe-key
storeFile=tictactoe-keystore.jks
EOF

# 查看文件内容（确认）
cat key.properties
```

#### 步骤 1.5：确认 .gitignore 已正确配置

**验证**签名相关文件不会被提交：

```bash
cd /remote-home/share/lijl/task_all/2XandO/tictactoe_game

# 检查 .gitignore 是否包含签名相关文件
grep -E "(jks|keystore|key\.properties|android-signing)" .gitignore
```

应该看到以下输出：
```
*.jks
*.keystore
key.properties
android-signing/
```

---

### 阶段二：GitHub Secrets 配置（约 20 分钟）

GitHub Actions 需要访问签名密钥和密码，但我们不能直接把这些敏感信息写在配置文件里。我们需要使用 GitHub 的 **Secrets** 功能。

#### 步骤 2.1：了解需要配置的 Secrets

我们需要配置以下 4 个 Secrets：

| Secret 名称 | 说明 | 示例值 |
|-------------|------|--------|
| `ANDROID_KEYSTORE_BASE64` | 密钥库文件的 Base64 编码 | （一长串字符）|
| `ANDROID_KEYSTORE_PASSWORD` | 密钥库密码 | `MyStrongPassword123` |
| `ANDROID_KEY_PASSWORD` | 密钥密码 | `MyStrongPassword123` |
| `ANDROID_KEY_ALIAS` | 密钥别名 | `tictactoe-key` |

#### 步骤 2.2：将密钥库文件转换为 Base64

GitHub Secrets 不能直接存储二进制文件（.jks），所以我们需要将其转换为 Base64 字符串。

```bash
cd /remote-home/share/lijl/task_all/2XandO/tictactoe_game

# base64 -w 0 表示：编码后不换行（重要！）
base64 -w 0 tictactoe-keystore.jks > keystore-base64.txt

# 查看生成的文件（前 100 个字符）
echo "Base64 内容（前 100 字符）："
head -c 100 keystore-base64.txt
echo ""
```

#### 步骤 2.3：在 GitHub 上配置 Secrets

**操作方法**：

1. **打开 GitHub 仓库页面**
   - 地址：`https://github.com/你的用户名/tictactoe-game`

2. **进入 Settings**
   - 点击页面顶部的 **Settings** 标签

3. **进入 Secrets and variables**
   - 左侧菜单找到 **Secrets and variables**
   - 点击 **Actions**

4. **添加新的 Secret**
   - 点击 **New repository secret** 按钮

5. **依次添加以下 4 个 Secrets**：

**Secret 1: ANDROID_KEYSTORE_BASE64**
- Name: `ANDROID_KEYSTORE_BASE64`
- Secret: 打开 `keystore-base64.txt` 文件，复制全部内容粘贴进去
- 点击 **Add secret**

**Secret 2: ANDROID_KEYSTORE_PASSWORD**
- Name: `ANDROID_KEYSTORE_PASSWORD`
- Secret: 你的密钥库密码（创建 keystore 时设置的 storePassword）
- 点击 **Add secret**

**Secret 3: ANDROID_KEY_PASSWORD**
- Name: `ANDROID_KEY_PASSWORD`
- Secret: 你的密钥密码（创建 keystore 时设置的 keyPassword，通常和 storePassword 相同）
- 点击 **Add secret**

**Secret 4: ANDROID_KEY_ALIAS**
- Name: `ANDROID_KEY_ALIAS`
- Secret: `tictactoe-key`（或你设置的别名）
- 点击 **Add secret**

6. **验证配置**
   - 完成后，你应该能看到 4 个 Secrets：
     - `ANDROID_KEYSTORE_BASE64`
     - `ANDROID_KEYSTORE_PASSWORD`
     - `ANDROID_KEY_PASSWORD`
     - `ANDROID_KEY_ALIAS`

---

### 阶段三：确认项目配置文件（约 10 分钟）

#### 步骤 3.1：检查 pubspec.yaml 版本号

当前版本：`version: 1.0.0+1`

**版本号格式说明**：
```
version: 1.0.0+1
         │   │  │
         │   │  └── versionCode（内部版本号，整数，每次发布必须递增）
         │   │
         │   └── 分隔符
         │
         └── versionName（显示给用户的版本名）
```

**v2.0 改进**：
- `versionName` 和 `versionCode` 主要由 Flutter 从 `pubspec.yaml` 读取
- **Git Tag** 用于：
  - 触发 Workflow（tag 匹配 `v*`）
  - 命名输出文件（如 `tictactoe-game-android-v1.0.3-release.apk`）
  - 命名 GitHub Release

**建议**：
- 保持 `pubspec.yaml` 中的版本号与 Tag 同步
- 每次发布时，递增 `+` 后面的 `versionCode`

#### 步骤 3.2：确认模板文件存在

检查 build.gradle 模板文件是否已创建：

```bash
cd /remote-home/share/lijl/task_all/2XandO/tictactoe_game

# 检查模板文件
ls -la config/android/app-build.gradle.template

# 查看模板文件的签名配置部分
grep -A 15 "signingConfigs" config/android/app-build.gradle.template
```

应该看到类似以下的输出：
```groovy
    signingConfigs {
        release {
            keyAlias keystoreProperties['keyAlias']
            keyPassword keystoreProperties['keyPassword']
            storeFile keystoreProperties['storeFile'] ? file(keystoreProperties['storeFile']) : null
            storePassword keystoreProperties['storePassword']
        }
    }
```

#### 步骤 3.3：关于 namespace 和 applicationId

**注意**：模板文件中使用了默认的包名：
- `namespace 'com.example.tictactoe_game'`
- `applicationId "com.example.tictactoe_game"`

**如果你需要自定义包名**：
1. 修改 `config/android/app-build.gradle.template` 中的 `namespace` 和 `applicationId`
2. 确保 `flutter create` 生成的 `AndroidManifest.xml` 中的包名匹配

**简化方案**：暂时使用默认的 `com.example.tictactoe_game`，这对于个人项目完全够用。

---

### 阶段四：统一 Workflow 详解

#### 4.1 文件位置
```
.github/workflows/build-all.yml
```

#### 4.2 Workflow 结构说明

这个统一的 Workflow 包含 **4 个 Job**，按以下顺序执行：

```
1. pre-check (预检查)
      │
      ├──▶ 2. build-windows (Windows 构建) ──┐
      │                                         │
      └──▶ 3. build-android (Android 构建) ────┼──▶ 4. create-release (创建 Release)
                                                │
                          (两个构建并行执行，都完成后才创建 Release)
```

#### 4.3 各 Job 详细说明

##### Job 1: pre-check（预检查）

**作用**：在实际构建前验证所有配置，提前发现问题。

**检查项**：
1. **验证 Tag 格式**：确保 tag 以 `v` 开头（如 `v1.0.3`）
2. **验证 pubspec.yaml**：确保包含版本号
3. **验证 Secrets**：确保所有 Android 签名 Secrets 已配置
4. **验证模板文件**：确保 `app-build.gradle.template` 存在

**代码位置**：`build-all.yml:27-106`

**优势**：
- 避免浪费构建时间在配置错误上
- 提前明确失败原因

##### Job 2: build-windows（Windows 构建）

**依赖**：`needs: pre-check`（预检查通过后才执行）

**步骤**：
1. 检出代码
2. 配置 Flutter
3. 安装依赖
4. 启用 Windows 桌面支持
5. 生成 Windows 平台文件
6. 构建 Release exe
7. 打包为 zip
8. 上传 Artifact（供后续 Job 使用）

**代码位置**：`build-all.yml:108-175`

**注意**：这个 Job **不直接创建 Release**，只上传 Artifact。

##### Job 3: build-android（Android 构建）

**依赖**：`needs: pre-check`（预检查通过后才执行）

**这是改进最多的 Job**，包含以下关键步骤：

###### 步骤 3.1：模板文件替换（v2.0 改进）

```yaml
- name: Copy build.gradle template
  run: |
    TEMPLATE="config/android/app-build.gradle.template"
    TARGET="android/app/build.gradle"
    
    if [ -f "$TEMPLATE" ]; then
      cp "$TEMPLATE" "$TARGET"
      echo "✅ build.gradle copied from template"
    else
      echo "❌ Template file not found"
      exit 1
    fi
```

**对比 v1.0 的 sed 方式**：

| 对比项 | v1.0 (sed) | v2.0 (模板替换) |
|--------|------------|-----------------|
| **可靠性** | 依赖生成的格式不变 | 完全可控，不依赖格式 |
| **维护性** | 复杂，需要调试正则 | 简单，直接编辑模板文件 |
| **灵活性** | 只能做简单替换 | 可以完全自定义 |
| **调试难度** | 高（sed 出错难定位） | 低（直接看模板文件） |

**代码位置**：`build-all.yml:219-236`

###### 步骤 3.2：配置签名

1. 从 Secrets 解码 keystore 文件到 `android/app/tictactoe-keystore.jks`
2. 创建 `android/key.properties` 文件

**代码位置**：`build-all.yml:238-271`

###### 步骤 3.3：签名验证（v2.0 新增）

```yaml
- name: Verify APK signature
  run: |
    APK_PATH="build/app/outputs/flutter-apk/app-release.apk"
    
    # 使用 apksigner 验证（Android SDK 官方工具）
    APK_SIGNER="$BUILD_TOOLS_DIR/$LATEST_BUILD_TOOLS/apksigner"
    if [ -f "$APK_SIGNER" ]; then
      if "$APK_SIGNER" verify --print-certs "$APK_PATH"; then
        echo "✅ APK signature verified successfully"
      fi
    fi
    
    # 备用方案：使用 jarsigner
    if command -v jarsigner &> /dev/null; then
      if jarsigner -verify "$APK_PATH" 2>/dev/null; then
        echo "✅ jarsigner verification: APK is signed"
      fi
    fi
```

**为什么需要签名验证？**
- 确保 `build.gradle` 中的签名配置生效
- 确认 APK 使用了正确的密钥签名
- 避免发布未签名或签名错误的 APK

**代码位置**：`build-all.yml:298-338`

###### 步骤 3.4：版本号自动管理（v2.0 改进）

```yaml
- name: Rename APK with version
  env:
    VERSION: ${{ needs.pre-check.outputs.tag }}
  run: |
    APK_VERSION=${VERSION:-dev}
    
    cp build/app/outputs/flutter-apk/app-release.apk \
      ${{ env.APP_NAME }}-android-$APK_VERSION-release.apk
```

**工作原理**：
1. `pre-check` Job 从 `github.ref_name` 提取 tag 名称
2. 将 tag 名称作为 output 传递
3. 本 Job 使用这个 tag 来命名 APK 文件

**示例**：
- Tag: `v1.0.3`
- 输出文件: `tictactoe-game-android-v1.0.3-release.apk`

**代码位置**：`build-all.yml:340-352`

##### Job 4: create-release（创建统一 Release）

**依赖**：`needs: [pre-check, build-windows, build-android]`

**关键特性**：
- 只有**前三个 Job 都成功**才会执行
- 统一下载两个平台的 Artifact
- 统一创建一个 GitHub Release
- 自动生成 Release Notes

```yaml
- name: Download Windows Artifact
  uses: actions/download-artifact@v7
  with:
    name: ${{ env.APP_NAME }}-Windows-ZIP
    path: ./release

- name: Download Android Artifact
  uses: actions/download-artifact@v7
  with:
    name: ${{ env.APP_NAME }}-Android-Release-APK
    path: ./release

- name: Create GitHub Release
  uses: softprops/action-gh-release@v2
  with:
    files: |
      ./release/*
    generate_release_notes: true
    token: ${{ secrets.GITHUB_TOKEN }}
```

**为什么这是最佳实践？**
1. **消除竞争条件**：只有一个 Job 负责创建 Release
2. **原子性**：要么双平台都发布，要么都不发布
3. **可追溯性**：Release 明确对应一次完整的构建
4. **统一的 Release Notes**：由一个 Job 生成，格式一致

**代码位置**：`build-all.yml:365-412`

---

## 五、验证步骤（约 30 分钟）

### 5.1 确认文件已就绪

```bash
cd /remote-home/share/lijl/task_all/2XandO/tictactoe_game

# 检查所有新增/修改的文件
echo "=== 检查文件 ==="

echo "1. 统一 Workflow:"
ls -la .github/workflows/build-all.yml

echo "2. build.gradle 模板:"
ls -la config/android/app-build.gradle.template

echo "3. .gitignore 更新:"
grep -A 4 "Android Signing" .gitignore

echo "4. pubspec.yaml 版本:"
grep "version:" pubspec.yaml
```

### 5.2 提交配置文件

```bash
cd /remote-home/share/lijl/task_all/2XandO/tictactoe_game

# 检查修改的文件
git status

# 添加需要提交的文件
git add pubspec.yaml
git add .github/workflows/build-all.yml
git add config/android/app-build.gradle.template
git add .gitignore
git add 安卓方案.md

# 不要添加这些！
# git add tictactoe-keystore.jks  ❌ 绝对不能
# git add key.properties           ❌ 绝对不能
# git add keystore-base64.txt      ❌ 绝对不能

# 提交
git commit -m "feat: 统一多平台构建方案 v2.0

改进内容：
- 合并 Windows 和 Android 为统一 Workflow
- 使用 Jobs 依赖消除竞争条件
- 新增预检查 Job，提前验证配置
- 改用模板文件替换 build.gradle（替代 sed）
- 新增 APK 签名验证步骤
- 版本号自动从 Git Tag 推导
- 统一由 create-release Job 创建 Release"

# 推送到远程
git push origin master
```

### 5.3 创建测试标签并触发构建

```bash
# 确保版本号已更新
# pubspec.yaml 中的 version 应该是类似：version: 1.0.3+3

# 创建标签（使用语义化版本）
git tag -a v1.0.3 -m "v1.0.3 - 统一多平台构建

新增功能：
- 统一 Workflow，消除竞争条件
- 预检查机制，提前发现配置问题
- 模板文件替换，更可靠的签名配置
- 签名验证，确保 APK 正确签名
- 自动版本号管理，从 Tag 推导"

# 推送标签
git push origin v1.0.3
```

### 5.4 观察构建过程

1. 打开 GitHub 仓库 → **Actions** 标签
2. 应该能看到 **一个** 工作流运行：
   - `Build All Platforms`（统一 Workflow）
3. 点击进入，查看 Job 执行顺序：

```
执行顺序可视化：

[时间轴] ─────────────────────────────────────────────────▶

pre-check ──────▶ 完成
     │
     ├──▶ build-windows ────────────────────▶ 完成 ──┐
     │                                                  │
     └──▶ build-android ─────────────────────▶ 完成 ──┼──▶ create-release ──▶ 完成
                                                        │
                                              (两个都完成后才开始)
```

4. 等待构建完成（约 15-25 分钟）

### 5.5 验证构建结果

#### 验证各 Job 执行

1. **pre-check**：确认所有检查项通过
2. **build-windows**：确认构建成功，Artifact 已上传
3. **build-android**：确认：
   - 模板文件复制成功
   - 签名配置正确
   - **签名验证步骤通过**（关键！）
   - Artifact 已上传
4. **create-release**：确认 Release 创建成功

#### 验证 Release 内容

1. 打开 GitHub 仓库 → **Releases** 标签
2. 找到最新的 Release（tag: v1.0.3）
3. 确认 **Assets** 中包含：
   - `tictactoe-game-windows.zip`（Windows 版本）
   - `tictactoe-game-android-v1.0.3-release.apk`（Android 版本）
4. 确认 **Release Notes** 已自动生成

---

## 六、本地安装测试（可选）

### 6.1 下载 APK

从 GitHub Release 页面下载 APK 文件到本地电脑。

### 6.2 传输到 Android 设备

- 使用 USB 数据线传输
- 或使用微信/QQ 等工具发送
- 或上传到云盘下载

### 6.3 安装 APK

1. 在 Android 设备上打开文件管理器
2. 找到 APK 文件
3. 点击安装
4. 如果提示"安装被阻止"：
   - 点击"设置"
   - 允许"来自此来源的应用"
   - 返回继续安装
5. 安装完成后打开游戏

### 6.4 测试功能

- 测试棋子放置
- 测试胜负判断
- 测试重置功能
- 测试界面适配（不同屏幕尺寸）

---

## 七、常见问题与解决方案

### 问题 1：pre-check Job 失败

**现象**：构建在 pre-check 阶段就失败了

**可能原因及解决方案**：

| 错误信息 | 原因 | 解决方案 |
|----------|------|----------|
| `Invalid tag format` | Tag 不是以 `v` 开头 | 确保 tag 格式为 `v1.0.3` |
| `No version found in pubspec.yaml` | pubspec.yaml 缺少 version 行 | 检查 pubspec.yaml，确保有 `version: x.x.x+x` |
| `ANDROID_KEYSTORE_BASE64 is not configured` | Secrets 未配置 | 回到"阶段二"，配置所有 4 个 Secrets |
| `build.gradle template not found` | 模板文件缺失 | 确认 `config/android/app-build.gradle.template` 已提交 |

**优势**：v2.0 的预检查让这些问题在构建开始前就暴露，节省时间。

### 问题 2：模板文件替换后的 namespace 问题

**现象**：构建时提示 `namespace` 不匹配

**原因**：
- `flutter create` 生成的 `AndroidManifest.xml` 中的包名
- 与模板 `build.gradle` 中的 `namespace` 不一致

**解决方案**：

**方案 A：修改模板文件（推荐）**

查看 `flutter create` 实际生成的 namespace：

```bash
# 本地测试：运行 flutter create，查看生成的 build.gradle
cd /remote-home/share/lijl/task_all/2XandO/tictactoe_game
flutter create . --platforms android
grep "namespace" android/app/build.gradle
```

然后修改模板文件 `config/android/app-build.gradle.template`，将 `namespace` 和 `applicationId` 改为实际值。

**方案 B：使用 sed 动态替换（备用）**

如果模板文件方式仍有问题，可以在 workflow 中使用 sed 调整 namespace：

```yaml
- name: Adjust namespace if needed
  run: |
    # 查看 flutter create 生成的实际 namespace
    ACTUAL_NAMESPACE=$(grep -oP "namespace '\K[^']+" android/app/build.gradle 2>/dev/null || echo "")
    echo "Actual namespace from flutter create: $ACTUAL_NAMESPACE"
    
    # 如果需要，可以在这里动态调整模板
```

### 问题 3：签名验证失败

**现象**：`Verify APK signature` 步骤失败

**可能原因**：

1. **Secrets 配置错误**
   - 检查 `ANDROID_KEYSTORE_BASE64` 是否正确（没有换行符）
   - 检查密码是否正确

2. **模板文件中的签名配置有误**
   - 检查 `app-build.gradle.template` 中的 `signingConfigs` 配置
   - 确认 `buildTypes.release` 使用了 `signingConfig signingConfigs.release`

3. **keystore 文件解码错误**
   - 验证 Base64 编码是否正确：
     ```bash
     # 本地验证
     base64 -d keystore-base64.txt > test-keystore.jks
     keytool -list -v -keystore test-keystore.jks
     ```

### 问题 4：create-release Job 未执行

**现象**：前两个 Job 成功，但 create-release 没有执行

**原因**：
- `needs: [pre-check, build-windows, build-android]` 要求所有依赖 Job 都成功
- 如果任意一个依赖 Job 失败或被跳过，create-release 不会执行

**检查**：
1. 确认 `pre-check`、`build-windows`、`build-android` 三个 Job 都是 **✓ Successful**
2. 确认没有 Job 被跳过（Skipped）

### 问题 5：密钥库密码忘记了怎么办？

**解决方案**：
- 如果还没有发布过应用，可以重新创建一个新的密钥库
- 如果已经发布过应用且用户已安装，**必须使用相同的密钥库**才能更新
- ⚠️ 重要提示：创建密钥库后立即备份，并将密码记录在安全的地方

### 问题 6：Android 版本号问题

**现象**：用户无法覆盖安装新版本

**原因**：`versionCode` 没有递增

**解决方案**：
- 每次发布前，修改 `pubspec.yaml`
- 将 `version: 1.0.0+1` 改为 `version: 1.0.3+3`
- 确保 `+` 后面的数字每次都递增

**注意**：v2.0 中，Git Tag 用于文件命名，**不影响** Android 内部的 `versionCode`。`versionCode` 仍然由 `pubspec.yaml` 决定。

### 问题 7：构建超时

**解决方案**：
在 workflow 中增加 `timeout-minutes`：
```yaml
jobs:
  build-android:
    timeout-minutes: 40  # 从默认的 30 增加到 40
```

---

## 八、文件模板详解

### 8.1 app-build.gradle.template 关键部分

模板文件位于：`config/android/app-build.gradle.template`

**核心签名配置**：

```groovy
// 在文件开头，加载 key.properties
def keystoreProperties = new Properties()
def keystorePropertiesFile = rootProject.file('key.properties')
if (keystorePropertiesFile.exists()) {
    keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
}

// 在 android 块内
android {
    // ... 其他配置 ...

    signingConfigs {
        release {
            keyAlias keystoreProperties['keyAlias']
            keyPassword keystoreProperties['keyPassword']
            storeFile keystoreProperties['storeFile'] ? file(keystoreProperties['storeFile']) : null
            storePassword keystoreProperties['storePassword']
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release  // 关键：使用 release 签名
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
```

**关键要点**：
1. `rootProject.file('key.properties')` → 从 `android/` 目录读取
2. `file(keystoreProperties['storeFile'])` → 相对于 `android/app/` 目录
3. `signingConfig signingConfigs.release` → release buildType 使用 release 签名

### 8.2 为什么需要 minifyEnabled？

```groovy
minifyEnabled true
```

**作用**：
- 启用代码混淆和压缩
- 减小 APK 体积
- 提高运行性能
- 增加反编译难度

**注意**：如果你的应用使用了某些特殊的反射或动态加载，可能需要在 `proguard-rules.pro` 中添加保留规则。

---

## 九、安全建议

### 9.1 密钥库安全

| 建议 | 说明 |
|------|------|
| **离线备份** | 将密钥库文件和密码记录在安全的地方 |
| **多份备份** | 备份到多个位置（U盘、加密云盘等）|
| **密码强度** | 使用强密码，包含大小写字母、数字、特殊字符 |
| **定期检查** | 确认备份文件可以正常使用 |

### 9.2 GitHub Secrets 安全

| 建议 | 说明 |
|------|------|
| **不要打印** | 不要在 workflow 中打印 Secrets 内容 |
| **最小权限** | 只给需要的人访问 Secrets 的权限 |
| **定期轮换** | 如果怀疑泄露，立即更新 Secrets 和密钥库 |

### 9.3 Git 提交安全

```bash
# 确认不会提交敏感文件
git status

# 应该看到的是：
# ❌ 不应该看到：tictactoe-keystore.jks, key.properties, keystore-base64.txt
# ✅ 应该看到：pubspec.yaml, build-all.yml, app-build.gradle.template, .gitignore
```

### 9.4 v2.0 的安全性增强

| 增强项 | 说明 |
|--------|------|
| **预检查** | 验证 Secrets 存在，但不会打印值 |
| **Redact** | workflow 中使用 `sed 's/=.*/=***REDACTED***/'` 隐藏密码 |
| **Artifacts** | 构建产物通过 Artifacts 传递，不暴露在日志中 |

---

## 十、与旧方案的兼容性

### 10.1 旧的 Workflow 文件

v1.0 的两个独立 workflow 文件：
- `.github/workflows/build-windows.yml`
- `.github/workflows/build-android.yml`

**处理建议**：

| 选项 | 操作 | 说明 |
|------|------|------|
| **保留（推荐）** | 不删除 | 作为备用，万一新方案有问题可以快速回滚 |
| **删除** | git rm | 保持仓库整洁，但需要确保新方案稳定 |
| **重命名** | 改为 `.yml.disabled` | 禁用但保留，更安全 |

### 10.2 触发条件

- **新方案**：`.github/workflows/build-all.yml` 监听 `tags: ['v*']`
- **旧方案**：两个文件也监听 `tags: ['v*']`

**⚠️ 重要**：
如果保留旧的 workflow 文件，**推送 tag 时会同时触发 3 个 workflow**：
1. `Build All Platforms`（新的）
2. `Build Windows Executable`（旧的）
3. `Build Android Release APK`（旧的）

**这可能导致混乱**！

**解决方案**：

**方案 A：删除或禁用旧文件（推荐）**

```bash
# 方案 A1：删除
git rm .github/workflows/build-windows.yml
git rm .github/workflows/build-android.yml

# 方案 A2：重命名禁用
mv .github/workflows/build-windows.yml .github/workflows/build-windows.yml.disabled
mv .github/workflows/build-android.yml .github/workflows/build-android.yml.disabled
git add .github/workflows/
```

**方案 B：修改旧文件的触发条件**

在旧文件的 `on` 部分添加条件，让它们只在特定情况下触发：

```yaml
# build-windows.yml 和 build-android.yml 中
on:
  workflow_dispatch:  # 只保留手动触发
  # 移除 push.tags 配置
```

### 10.3 回滚方案

如果新方案出现问题，可以快速回滚：

1. 恢复旧的 workflow 文件
2. 或者使用 `workflow_dispatch` 手动触发旧的 workflow

---

## 十一、实施清单

| 序号 | 任务 | 状态 | 说明 |
|------|------|------|------|
| 1 | 安装/检查 Java JDK | ⬜ 待执行 | keytool 需要 Java |
| 2 | 创建密钥库文件 (.jks) | ⬜ 待执行 | 记住密码！ |
| 3 | Base64 编码密钥库 | ⬜ 待执行 | `base64 -w 0` |
| 4 | 配置 GitHub Secrets | ⬜ 待执行 | 4 个 Secrets |
| 5 | 确认 pubspec.yaml 版本号 | ⬜ 待执行 | `version: x.x.x+x` |
| 6 | 确认模板文件已提交 | ✅ 已完成 | `config/android/app-build.gradle.template` |
| 7 | 确认统一 Workflow 已提交 | ✅ 已完成 | `.github/workflows/build-all.yml` |
| 8 | 确认 .gitignore 已更新 | ✅ 已完成 | 添加了签名文件忽略 |
| 9 | 处理旧的 workflow 文件 | ⬜ 待执行 | 删除或禁用 |
| 10 | 提交并推送所有配置 | ⬜ 待执行 | `git add ... && git commit ...` |
| 11 | 创建测试标签 | ⬜ 待执行 | `git tag -a v1.0.3 ...` |
| 12 | 推送标签触发构建 | ⬜ 待执行 | `git push origin v1.0.3` |
| 13 | 验证 pre-check 通过 | ⬜ 待执行 | 检查 Actions |
| 14 | 验证双平台构建成功 | ⬜ 待执行 | 检查 Artifacts |
| 15 | 验证签名验证通过 | ⬜ 待执行 | Android Job 中的步骤 |
| 16 | 验证统一 Release 创建 | ⬜ 待执行 | 检查 Releases 页面 |
| 17 | 验证 Release 包含双平台 | ⬜ 待执行 | 确认两个文件都在 |

---

## 十二、时间估算

| 阶段 | 预计时间 | 说明 |
|------|---------|------|
| 阶段一：本地准备（创建 keystore） | 30 分钟 | 交互式操作 |
| 阶段二：GitHub Secrets 配置 | 20 分钟 | Web 界面操作 |
| 阶段三：确认项目配置 | 10 分钟 | 检查文件 |
| 阶段四：提交并推送 | 10 分钟 | Git 操作 |
| 阶段五：等待构建 | 15-25 分钟 | 自动执行 |
| 阶段六：验证结果 | 10 分钟 | 检查 Actions 和 Releases |
| **总计** | **约 1.5-2 小时** | 实际操作时间 |

---

## 十三、预期成果

实施完成后，您将获得：

1. **零竞争条件**：统一 Workflow + Jobs 依赖，确保 Release 只创建一次
2. **提前发现问题**：pre-check Job 在构建前验证所有配置
3. **可靠的签名配置**：模板文件替换，不依赖 `flutter create` 的格式
4. **签名验证保障**：构建后自动验证 APK 签名正确性
5. **自动版本管理**：从 Git Tag 自动推导版本号
6. **原子性发布**：要么双平台都发布，要么都不发布
7. **统一的用户体验**：用户从同一个 Release 页面下载双平台版本

---

## 十四、后续可执行操作步骤

### 步骤 1：处理旧的 workflow 文件（重要！）

**推荐方案：重命名禁用**

```bash
cd /remote-home/share/lijl/task_all/2XandO/tictactoe_game

# 重命名旧的 workflow 文件，使其不再被 GitHub Actions 识别
mv .github/workflows/build-windows.yml .github/workflows/build-windows.yml.disabled
mv .github/workflows/build-android.yml .github/workflows/build-android.yml.disabled

# 查看修改
ls -la .github/workflows/

# 提交更改
git add .github/workflows/
git commit -m "chore: 禁用旧的独立 workflow 文件

使用统一的 build-all.yml 替代，避免推送 tag 时触发多个 workflow"
git push origin master
```

### 步骤 2：创建密钥库（如果还没有）

**如果已经创建了密钥库，请跳过此步骤。**

```bash
cd /remote-home/share/lijl/task_all/2XandO/tictactoe_game

# 检查是否已有 keystore
ls -la *.jks 2>/dev/null || echo "没有找到 .jks 文件，需要创建"

# 如果没有，创建密钥库
keytool -genkey -v \
  -keystore tictactoe-keystore.jks \
  -alias tictactoe-key \
  -keyalg RSA \
  -keysize 2048 \
  -validity 36500
```

### 步骤 3：Base64 编码密钥库

```bash
cd /remote-home/share/lijl/task_all/2XandO/tictactoe_game

# Base64 编码（不换行）
base64 -w 0 tictactoe-keystore.jks > keystore-base64.txt

# 验证编码（可选）
echo "编码文件大小: $(wc -c keystore-base64.txt | awk '{print $1}') 字节"
```

### 步骤 4：配置 GitHub Secrets

在 GitHub 网页界面配置以下 4 个 Secrets：

| Secret 名称 | 值来源 |
|-------------|--------|
| `ANDROID_KEYSTORE_BASE64` | `keystore-base64.txt` 文件的全部内容 |
| `ANDROID_KEYSTORE_PASSWORD` | 创建 keystore 时设置的密码 |
| `ANDROID_KEY_PASSWORD` | 通常与 keystore 密码相同 |
| `ANDROID_KEY_ALIAS` | `tictactoe-key`（或你设置的别名） |

### 步骤 5：确认所有文件已提交

```bash
cd /remote-home/share/lijl/task_all/2XandO/tictactoe_game

# 检查 git 状态
git status

# 应该看到的已提交文件：
# - .github/workflows/build-all.yml
# - .github/workflows/*.disabled（如果重命名了）
# - config/android/app-build.gradle.template
# - .gitignore
# - 安卓方案.md
# - pubspec.yaml（如果修改了版本号）

# 不应该看到：
# - tictactoe-keystore.jks
# - key.properties
# - keystore-base64.txt
```

### 步骤 6：更新 pubspec.yaml 版本号

```bash
cd /remote-home/share/lijl/task_all/2XandO/tictactoe_game

# 查看当前版本
grep "version:" pubspec.yaml

# 如果需要，修改 versionCode（+ 后面的数字）
# 例如：从 version: 1.0.0+1 改为 version: 1.0.3+3
```

### 步骤 7：创建并推送测试标签

```bash
cd /remote-home/share/lijl/task_all/2XandO/tictactoe_game

# 确保所有更改已提交
git status

# 创建标签（使用语义化版本）
git tag -a v1.0.3 -m "v1.0.3 - 统一多平台构建

统一 Workflow 改进：
- 消除竞争条件
- 新增预检查
- 模板文件替换
- 签名验证
- 自动版本号"

# 推送标签
git push origin v1.0.3
```

### 步骤 8：在 GitHub 上监控构建

1. 打开 `https://github.com/你的用户名/tictactoe-game/actions`
2. 找到名为 `Build All Platforms` 的 workflow run
3. 点击进入，查看执行进度：
   - 首先执行：`pre-check`
   - 然后并行执行：`build-windows` 和 `build-android`
   - 最后执行：`create-release`（只有前两个都成功）

### 步骤 9：验证构建结果

#### 验证各 Job 状态

| Job | 需要检查的内容 |
|-----|----------------|
| `pre-check` | 显示"✅"，没有错误 |
| `build-windows` | 显示"✅"，Artifact 已上传 |
| `build-android` | 显示"✅"，找到"APK signature verified successfully" |
| `create-release` | 显示"✅" |

#### 验证 Release

1. 打开 `https://github.com/你的用户名/tictactoe-game/releases`
2. 找到 `v1.0.3` 版本
3. 确认 **Assets** 包含：
   - `tictactoe-game-windows.zip`
   - `tictactoe-game-android-v1.0.3-release.apk`

### 步骤 10：本地测试（可选）

下载 APK 到 Android 设备，测试安装和运行。

---

## 十五、紧急回滚方案

如果新方案出现问题，可以快速回滚：

### 方案 A：恢复旧的 workflow 文件

```bash
cd /remote-home/share/lijl/task_all/2XandO/tictactoe_game

# 恢复重命名的文件
mv .github/workflows/build-windows.yml.disabled .github/workflows/build-windows.yml
mv .github/workflows/build-android.yml.disabled .github/workflows/build-android.yml

# 提交
git add .github/workflows/
git commit -m "revert: 回滚到旧的独立 workflow"
git push origin master
```

### 方案 B：使用 workflow_dispatch 手动触发

在 GitHub Actions 页面，手动触发旧的 workflow：
1. 打开 Actions 页面
2. 选择旧的 workflow（如 `Build Windows Executable`）
3. 点击 `Run workflow` 按钮

---

**请按照上述步骤操作。如果遇到任何问题，请提供具体的错误信息，我会帮您解决！**

---

## 附录：完整的文件列表

### 新增/修改的文件

| 文件路径 | 状态 | 说明 |
|----------|------|------|
| `.github/workflows/build-all.yml` | ✅ 新增 | 统一 Workflow |
| `.github/workflows/build-windows.yml` | ⚠️ 需处理 | 旧的 Windows workflow（建议禁用） |
| `.github/workflows/build-android.yml` | ⚠️ 需处理 | 旧的 Android workflow（建议禁用） |
| `config/android/app-build.gradle.template` | ✅ 新增 | build.gradle 签名模板 |
| `.gitignore` | ✅ 已修改 | 添加签名文件忽略规则 |
| `安卓方案.md` | ✅ 已修改 | 本文档（v2.0 改进版） |
| `pubspec.yaml` | ⚠️ 需确认 | 版本号可能需要更新 |

### 不应提交的文件

| 文件名 | 说明 | 为什么不应提交 |
|--------|------|----------------|
| `tictactoe-keystore.jks` | 密钥库文件 | 包含私钥，泄露风险 |
| `key.properties` | 签名配置 | 包含密码，泄露风险 |
| `keystore-base64.txt` | Base64 编码 | 可解码为私钥，泄露风险 |
| `android/` | 动态生成的平台文件 | `flutter create` 会重新生成 |
| `windows/` | 动态生成的平台文件 | `flutter create` 会重新生成 |
| `build/` | 构建输出 | 临时文件 |
